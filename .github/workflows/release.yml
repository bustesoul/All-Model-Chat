name: 'Release Desktop App'

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  release:
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: 'macos-latest'
            args: '--target universal-apple-darwin'
          - platform: 'ubuntu-22.04'
            args: ''
          - platform: 'windows-latest'
            args: ''

    runs-on: ${{ matrix.platform }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'
          cache-dependency-path: all-model-chat/package-lock.json

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.platform == 'macos-latest' && 'aarch64-apple-darwin,x86_64-apple-darwin' || '' }}

      - name: Install dependencies (Ubuntu only)
        if: matrix.platform == 'ubuntu-22.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.0-dev libwebkit2gtk-4.1-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install frontend dependencies
        run: |
          cd all-model-chat
          npm ci

      - name: Rust cache
        uses: swatinem/rust-cache@v2
        with:
          workspaces: './src-tauri -> target'

      - name: Build and Release
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tagName: ${{ github.ref_name }}
          releaseName: 'All Model Chat ${{ github.ref_name }}'
          releaseBody: |
            ## 🎉 Release Notes

            ### 下载 / Download
            请根据您的操作系统选择对应的安装包：
            - **Windows**: `.exe` 或 `.msi` 文件
            - **macOS**: `.dmg` 文件 (支持 Intel 和 Apple Silicon)
            - **Linux**: `.AppImage` 或 `.deb` 文件

            ### 更新内容 / What's New
            查看完整的更新日志，请访问 [Commits](https://github.com/${{ github.repository }}/commits/${{ github.ref_name }})

            ### 安装说明 / Installation
            1. 下载适合您系统的安装包
            2. 双击安装包进行安装
            3. 首次运行时在设置中填入您的 Gemini API Key

            ---
            **Full Changelog**: https://github.com/${{ github.repository }}/compare/${{ github.ref_name }}
          releaseDraft: false
          prerelease: false
          args: ${{ matrix.args }}
